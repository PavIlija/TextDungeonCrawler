#include "Player.h"

Player::Player(string n,int h,int a,int d)
GameCharacter(string n,int h,int a, int d)
{
    name=n;
    currentHealth=h;
    maxHealth=h;
    attack=a;
    defence=d;
};

    void addItem(Item is)
    {
        
    }
            void increaseStats(int,int,int);

            void lootRoom(Room *);

            void changeRooms(Room);